%ul.pagination.pagination-lg(ng-show="totalPages > 1")
  %li(ng-repeat="page in pages track by $index" ng-class="{active: page.active, disabled: page.disabled}")
    %a(href ng-click="selectPage(page.number)") {{page.text}}
